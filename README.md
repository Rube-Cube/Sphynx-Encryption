# I dunno, I'm new to this.
don't expect this to be a masterclass in optimization. there's going to be a lot of waste processing in converting data from one type to another just to convert it back right away. I only intended to make the functions easier to work with in the short term as I intend this to be a small project.

maybe in the future I will abstract the type conversion to separate functions to keep the input and output types of the processing functions the same.

I ask for kind correction if I misuse any terminology in the codebase (e.g. "hash").

Currently does not work in the CLI, I assume this has to do with how UTF-8 encoding works. will determine if this needs to be fixed later on.
